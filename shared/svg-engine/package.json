{
  "name": "@mendix-svg/svg-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic SVG.js surface for Mendix chart widgets (scaffold).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "jest --config jest.config.cjs"
  },
  "dependencies": {
    "@svgdotjs/svg.js": "^3.2.4"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.4.1",
    "jest-environment-jsdom": "^29.4.1",
    "ts-jest": "^29.2.5"
  }
}
